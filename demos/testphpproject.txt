#
# TCP Test Suite Driver
# $Id$

# Input language described in docs/pageunit.html

H calendar.phenogenomics.ca
O 80

U /index.php
# This login page has almost no distinctive text, so don't use M here.
F frm
# Use username & pass from config file, so that, like the hostname
# and port, these values are not baked into the test suite.
R loginstring=${USER}
R user_pw=${PASS}
# submit the form using its default button (PHPProject doesn't name the form button)
S

# All done.
U /index.php?module=logout